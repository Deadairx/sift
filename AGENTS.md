## Commit Messages

- When work is associated with a known ticket or issue, include that ticket number in the commit message. Use a closing keyword such as `Closes #6` when the commit completes the ticket.
